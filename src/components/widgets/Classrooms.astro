---
import Grid from '~/components/common/Grid.astro';
import WidgetWrapper from '~/components/ui/WidgetWrapper.astro';
import type {IItem} from '~/components/common/Grid.astro';
import type { Widget } from '~/types';

export interface Props extends Widget {
  title?: string;
  information?: string;
  items: Array<IItem>;
}

const {
  title = await Astro.slots.render('title'),
  information = await Astro.slots.render('information'),
  items,
  id,
  isDark = false,
  classes = {},
  bg = await Astro.slots.render('bg'),
} = Astro.props;

---

<WidgetWrapper id={id} isDark={isDark} containerClass={classes?.container as string} bg={bg}>
  <div class="flex flex-col lg:justify-between lg:flex-row mb-8" data-animate="animate-fade-in-up">
    {title && (
      <div class="md:max-w-sm">
        <h2
          class="text-3xl font-bold tracking-tight sm:text-4xl sm:leading-none group font-heading mb-2 text-white dark:text-white"
          set:html={title}
        />
      </div>
    )}

    {information && <p class="text-emerald-100 dark:text-slate-300 text-base lg:text-base lg:max-w-md leading-relaxed" set:html={information} />}
  </div>
  <Grid items={items} />
</WidgetWrapper>
