---
import { UI } from 'app:config';

// TODO: This code is temporary
---

<script is:inline define:vars={{ defaultTheme: UI.theme || 'light' }}>
  function applyTheme(theme) {
    if (theme === 'dark') {
      document.documentElement.classList.add('dark');
    } else {
      document.documentElement.classList.remove('dark');
    }
    const matches = document.querySelectorAll('[data-aw-toggle-color-scheme] > input');

    if (matches && matches.length) {
      matches.forEach((elem) => {
        elem.checked = theme !== 'dark';
      });
    }
  }

  // Modo claro por defecto para todos los visitantes. Solo se activa dark si el usuario lo configuró manualmente.
  if (defaultTheme && defaultTheme.endsWith(':only')) {
    applyTheme(defaultTheme.replace(':only', ''));
  } else if (localStorage.theme === 'dark') {
    applyTheme('dark');
  } else {
    applyTheme('light');
  }
</script>
